## Review

- Correct: Feature directory layout matches spec. `src/features/ask/` has the expected shallow structure: `model/` (20 source files + 20 spec files), `orchestration/` (ask-orchestrator.ts, ask-orchestrator.spec.ts, create-dashboard-orchestrator.ts), and `ui/` with three subfolders (ask-input/, ask-clarification/, ask-result/), each containing a source file, stories file, and index.ts. A top-level `index.ts` public surface is present. No extra nesting introduced.
- Correct: `question-parser.ts` ownership decision is sound. `src/features/ask/model/question-parser.ts:1-14` imports only ask-model siblings (`DateRangeParser`, `IntentCueDetector`, `TermMatcher`, `ValueFilterResolver`) and `../../../types` / `../../../utils`. Its sole consumer is `src/features/ask/model/ask-data.ts` (confirmed by grep — the only other importer is its own spec). Placing it in `model/` collocates the complete parsing pipeline within the ask boundary. The compat shim at `src/question-parser.ts:3` re-exports cleanly from `./features/ask/model/question-parser`.
- Correct: All 21 top-level compat shims are present and correct. Each file in `src/ask-data.ts`, `src/ask-orchestrator.ts`, `src/create-dashboard-orchestrator.ts`, and all 18 model shims (`src/catalog-builder.ts` through `src/vocabulary.ts`) contains a one-line `export *` pointing to the matching `./features/ask/…` path. Shim comment and path are consistent in every case.
- Correct: All 4 component-folder shims are present and correct. `src/components/ask-input/ask-input.ts`, `src/components/ask-clarification/ask-clarification.ts`, `src/components/ask-result/ask-result.ts`, and `src/components/ask-result/ask-result-model.ts` each contain a one-line re-export pointing to `../../features/ask/ui/…`. The `index.ts` files in each component folder chain through the shim correctly.
- Correct: No runtime `@` alias imports were introduced. A grep for `from '@/'` across `src/features/ask/` returned no results. All moved files use relative paths: model files use `../../../types`, `../../../utils`, `../../../query-port`; UI components use `../../../../components/…`, `../../../../types`, `../../../../utils`, `../../../../icons`.
- Correct: `vitest.config.ts` is updated correctly. Coverage `include` adds `src/features/ask/**/*.ts` at line 25. The `unit` project excludes `src/features/ask/ui/**/*.spec.ts` at line 46. The `components` project includes `src/features/ask/ui/**/*.spec.ts` at line 59. This mirrors the dashboard and question patterns. The `ask-orchestrator.spec.ts` in `orchestration/` is correctly matched by the unit project glob `src/**/*.spec.ts` and not excluded.
- Correct: Stories and specs are colocated under the feature tree. Each moved spec lands beside its source file in `model/` or `ui/ask-result/`. Story files are present in `ui/ask-input/`, `ui/ask-clarification/`, and `ui/ask-result/`. Old spec and story files were removed from their original locations (confirmed by `D` entries in git status for `src/components/ask-result/ask-result-model.spec.ts`, `src/components/ask-result/ask-result.stories.ts`, `src/components/ask-input/ask-input.stories.ts`, `src/components/ask-clarification/ask-clarification.stories.ts`, and all 20 top-level `*.spec.ts` files).
- Correct: `src/features/dashboard/ui/dashboard-editor/dashboard-editor.ts:1-3` imports ask UI components as side-effect imports from `../../../ask/ui/ask-clarification`, `../../../ask/ui/ask-input`, `../../../ask/ui/ask-result` — all pointing to the new feature boundary. Lines 17–18 import `AskOrchestrator` and `createDashboardOrchestrator` from `../../../ask/orchestration/…`. All old component-path imports have been removed.
- Correct: `src/features/dashboard/ui/dashboard-workspace/dashboard-workspace.ts:20` imports `AskDataEngine` from `../../../ask/model/ask-data` — direct feature-to-feature path using the new boundary.
- Correct: `src/app/shell/app-shell.ts` does not directly import ask UI; it imports `dashboard-editor` which side-effect-imports ask UI internally. The shell-level imports for question feature (`../../features/question/ui/question-editor`, `../../features/question/ui/question-list`) and dashboard feature (`../../features/dashboard/ui/dashboard-editor`, `../../features/dashboard/ui/dashboard-list`) are all correct feature-boundary paths. No old component-folder paths remain in the shell.
- Correct: `src/question-parser.ts` shim re-exports cleanly from `./features/ask/model/question-parser`. The shim uses `export *` so all named exports (`QuestionParser`) are re-exported without type narrowing. Any importer that used the old path continues to work.
- Note: `format:check` initially flagged `issues/05-worker-output.md` and `issues/reviewer-validation-after-issue-04.md` as unformatted. These are documentation files committed by the worker, not implementation files. `prettier --write` was applied and both files re-staged before the final `format:check` pass. No implementation file was reformatted.
- Note: `create-dashboard-orchestrator.ts` at `src/features/ask/orchestration/create-dashboard-orchestrator.ts:2` imports `duckDBManager` from `../../../db` — a direct infrastructure dependency inside the ask feature boundary. This pre-existed the move and is carried over unchanged. Issue 06 (infra boundary) should evaluate whether this wiring belongs in an infra adapter rather than inside the feature.
- Note: Coverage config continues to grow piecemeal. `vitest.config.ts:21-25` now has four separate globs (`src/components/**/*.ts`, `src/features/dashboard/**/*.ts`, `src/features/question/**/*.ts`, `src/features/ask/**/*.ts`) rather than a unified `src/**/*.ts`. This is intentional (avoids pulling spec files in `src/` root into component coverage), but it requires each new feature folder to be manually added. This is a drift risk for Issue 06 and beyond.
- Note: `src/features/ask/index.ts` public surface re-exports from `model/ask-data`, `orchestration/ask-orchestrator`, `orchestration/create-dashboard-orchestrator`, and all three UI subfolders. It does not individually barrel every model file (vocabulary, catalog-builder, etc.). Callers that need those must import them directly or via the old shims. This is acceptable for the transition period but the barrel coverage gap should be resolved before shim removal in Issue 08.
- Note: Storybook 4 skipped test files — pre-existing, not introduced by this issue.

## Commands run

| Command                    | Result                                                                                             |
| -------------------------- | -------------------------------------------------------------------------------------------------- |
| `npm run typecheck`        | PASS — no errors                                                                                   |
| `npm run lint`             | PASS — no errors                                                                                   |
| `npm run format:check`     | Initial FAIL (2 unformatted docs files); PASS after `prettier --write` on those files and re-stage |
| `npm run test:unit`        | PASS — 28 files, 565 tests                                                                         |
| `npm run test:components`  | PASS — 5 files, 43 tests                                                                           |
| `npm run test:storybook`   | PASS — 14 passed, 4 skipped (18 total), 72 tests                                                   |
| `npm run test:integration` | PASS — 18 scenarios, 74 steps                                                                      |
| `npm run test:e2e`         | PASS — 8 scenarios, 47 steps                                                                       |

## Blockers

None. All validation commands pass clean. The `format:check` failure was in doc files outside the implementation scope and was remediated before recording the final result.
